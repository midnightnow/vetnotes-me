node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test
# Public Firebase web config only (VITE_FIREBASE_API_KEY — public by design).
# Committed so local and container builds both bake a working client bundle.
# Real secrets (VITE_GEMINI_API_KEY) stay in .env, which remains ignored.
!.env.production

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Firebase & build artifacts
.firebase/
vetcpd-dist/
dist/


# SSR (adapter-node) build output
build-ssr/
